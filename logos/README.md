# AOXC Logos

Place wallet-grade verified brand assets in this folder.

Recommended files:
- `aoxc-token.png`
- `aoxc-id.png`

Current Move display profiles reference these paths as logical asset URIs.


## Brand Governance
- Recommended canonical assets: `aoxc-token.png`, `aoxc-id.png`.
- Production policy: publish immutable content-addressed URIs and store checksum snapshots in governance records.
- Any branding metadata update should require DAO authorization + checksum equality checks on-chain.
